# Python DSA Learning - Requirements
# These packages help prevent worker process issues

# Core packages for safe execution
psutil>=5.8.0
multiprocess>=0.70.0

# For data visualization (if needed)
matplotlib>=3.5.0

# For better process management
concurrent-futures>=3.1.1

# Optional: For Jupyter environments
jupyter>=1.0.0
ipykernel>=6.0.0
